apiVersion: skaffold/v2beta19
kind: Config
metadata:
  name: client-service
build:
  artifacts:
  - image: client-service
    docker:
      dockerfile: Dockerfile
deploy:
  kubectl:
    manifests:
    - k8/deployment.yaml
